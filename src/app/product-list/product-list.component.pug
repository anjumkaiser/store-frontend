div("*ngFor"="let product of products")
        | Id : {{product.id}}
        | Name: {{product.name}}
        | Description {{product.description}}
        | Rating: {{product.rating}}

        button((click)="add_product(product)") Click me 